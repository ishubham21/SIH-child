const config = {
  baseUrl: "https://sih-backend-rtu-alpha.vercel.app", //no trialing slash here
  authUrl: "auth/child",
  dataUrl: "child/data",
  complete: "child/complete"
};
export default config;
